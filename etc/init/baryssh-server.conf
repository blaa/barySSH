description "BarySSH server daemon"

start on (local-filesystems and net-device-up IFACE!=lo)
stop on runlevel [!2345]

respawn

kill timeout 20

script
	if [ -f /etc/baryssh-server.conf ]; then
		. /etc/baryssh-server.conf
	fi
	exec baryssh
end script
